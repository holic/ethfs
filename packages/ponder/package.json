{
  "name": "ponder",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start",
    "codegen": "ponder codegen",
    "lint": "eslint .",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@ethfs/contracts": "workspace:*",
    "@ethfs/deploy": "workspace:*",
    "@ponder/core": "^0.1.0-next.12",
    "viem": "^1.19.9"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.54.0",
    "eslint-config-ponder": "^0.1.0-next.12",
    "typescript": "^5.3.2"
  },
  "trustedDependencies": [
    "better-sqlite3"
  ],
  "engines": {
    "node": ">=18"
  }
}
