import { UIWindow } from "../ui/UIWindow";

export const FileUploader = () => {
  return (
    <UIWindow
      id="FileUploader"
      titleBar={<>File Uploader</>}
      statusBar={<></>}
      initialX={180}
      initialY={280}
      initialWidth={700}
      initialHeight={400}
    >
      hello
    </UIWindow>
  );
};
